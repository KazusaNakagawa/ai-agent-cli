requests
yfinance
python-dotenv
notion-client
pytest
pytest-cov

# Web API (Phase 1)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
keyring>=24
httpx>=0.27
pytest-asyncio>=0.23

# Local LLM (experimental)
chromadb>=0.5
ollama>=0.3
# Force opentelemetry proto deps to a modern version so chromadb's grpc exporter
# is compatible with protobuf 5+ (uv otherwise resolves to ancient 1.11.x).
opentelemetry-proto>=1.30
opentelemetry-exporter-otlp-proto-grpc>=1.30

# Article body extraction for local-LLM briefing (#151)
trafilatura>=1.12

# Claude API briefing cost-verification spike (#204)
anthropic>=0.40
