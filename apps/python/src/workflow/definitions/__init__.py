"""Workflow definitions.

One module per workflow. Dropping a module in here registers it — the registry
imports every module in this package and collects the ``Workflow`` instances it
finds, so there is no separate table to update.
"""
