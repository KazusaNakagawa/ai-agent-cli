今日の日付: $today
関心テーマ: $themes

## これまでに生成した本文（参考）

$prior_text

---

あなたは私のポートフォリオマネージャーです。上の本文と関心テーマを踏まえ、下の 3 ブロックを `### {ブロック名}` 見出しで出力してください。

## 自分への示唆

### 買い増し候補
本文の材料から強気に傾く銘柄を 1-3 件、`- {銘柄} — {根拠 1 行}` の箇条書きで。該当なしは `- (なし)`。

### 注意銘柄
下振れ・利益確定を検討すべき銘柄を 1-3 件、`- {銘柄} — {根拠 1 行}` の箇条書きで。該当なしは `- (なし)`。

### 監視イベント
今後注視すべきイベント・指標（決算、FOMC、規制、地政学など）を 1-3 件、`- {イベント} — {なぜ重要か 1 行}` の箇条書きで。

**出力ルール（このルール自体は出力に含めないこと）:**
- 出力の **最上段は `## 自分への示唆` の見出し** から始め、上の 3 つの `### ` ブロックだけを書くこと。
- 各ブロックは 1 件 1 行の箇条書きにし、行頭の `- ` は 1 つだけにすること。
- 本文と学習知識から解釈してよいが、事実の創作は避けること。
- URL は書かないこと（本文に存在しない URL を作るのは禁止）。
- 挙げる銘柄は保有・注目銘柄を中心に、本文で言及された範囲にとどめること。
- 保有銘柄テーブルで「具体的なニュースは検索でも確認できず」と記載された銘柄は、買い増し候補・注意銘柄のいずれにも挙げないこと。市場全体の動向のみを根拠に特定銘柄を推薦・警告しないこと。
- 入力側の見出しや、この「出力ルール」「これまでに生成した本文」などの注記をそのままコピーしないこと。

---

このセクションだけを出力してください。前置き・末尾の挨拶・他のセクションは書かないこと。
