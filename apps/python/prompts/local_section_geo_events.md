今日の日付: $today
保有・注目銘柄: $tickers

## 検索結果（あなたが本文で引用してよい唯一の出典）

$web_context

---

上の「## 検索結果」だけを出典として、下の構造で **そのまま** 出力してください。

## 地政学トピック
- 「### 地政学トピック」ブロックの **各 `**トピック名**` ごと** に `### {トピック名}` 小見出しを作り、1-2 行で要約。
- 出典 URL は同ブロックから **一字一句そのまま** 引用 (`[出典](URL)` 形式)。

### 厳守事項（保有銘柄への波及判定）
- **必ず保有銘柄 ($tickers) のいずれかに直接影響するかを判定** し、各トピックの最後に **次のいずれか 1 行** を必ず書くこと:
  - 影響ありの場合: `- 保有銘柄への波及: {銘柄ティッカー} — {1 行の理由}`（複数あればカンマ区切り）
  - 影響なしの場合: `- 保有銘柄への波及: なし — {そう判断した 1 行の理由}`
- 「影響なし」とだけ書くのは禁止。理由を必ず添えること。
- 半導体・AI・サプライチェーン・原油・防衛・金融制裁などのテーマは $tickers の関連銘柄に **間接的にでも** 影響することが多いため、安易に「なし」と書かないこと。

### その他
- 検索ヒットなしのトピックは `### {トピック名}` の下に「(検索でも確認できず)」とだけ書き、波及判定行はスキップしてよい。
- 「### 地政学トピック」ブロックが入力に存在しなければ、このセクションごと省略すること。

## 監視イベント
- 「### 監視イベント」ブロックの **各 `**イベント名**` ごと** に `- {イベント名}: 1 行ステータス [出典](URL)` 形式で 1 行ずつ書く。
- URL は同ブロックから **一字一句そのまま** 引用。
- 検索ヒットなしのイベントは出典を `-` にして「(検索でも確認できず)」と書く。
- 「### 監視イベント」ブロックが入力に存在しなければ、このセクションごと省略すること。

---

この 2 セクションだけを出力してください。前置き・末尾の挨拶・他のセクションは書かないこと。
